# 📋 TODO — Ready for OpenClaw Execution

_Generated by Antigravity session 2026-03-06. Read GHOST_HANDOFF.md for full context._

## Priority 1: Deploy Ebook Checkout to Vultr

**Goal:** Get `landing/ebook_checkout.py` running on Vultr behind Apache SSL proxy.

**Steps:**

1. Create `landing/Dockerfile.ebook` — `python:3.12-slim`, install `stripe fastapi uvicorn`, expose port 8300
2. Add ebook service to Docker Compose (or run standalone): `uvicorn ebook_checkout:app --host 0.0.0.0 --port 8300`
3. Set `STRIPE_SECRET_KEY` env var (value is in `secrets.env` or VaultGuard)
4. Add Apache vhost proxy: `api.mphinance.com/api/ebook/*` → `localhost:8300`
5. Test: `curl https://mphinance.com/api/ebook/checkout` should redirect to Stripe

**Files:** `landing/ebook_checkout.py`, `landing/ebook/the-agentic-traders-playbook.html`

---

## Priority 2: Wire auto_backtest.py into Pipeline

**Goal:** Make backtesting run automatically as Stage 14 of the daily pipeline.

**Steps:**

1. In `dossier/generate.py`, after the report generation stage, add a call to `auto_backtest.py`
2. The backtest script already exists at `dossier/backtesting/auto_backtest.py`
3. It reads `daily_picks.json`, fetches forward returns (1d, 5d, 10d, 21d), appends to `backtesting/track_record.json`
4. Make it idempotent — don't re-check picks that already have return data
5. Test: `python3 dossier/backtesting/auto_backtest.py` should run without errors

**Files:** `dossier/generate.py`, `dossier/backtesting/auto_backtest.py`

---

## Priority 3: GA4 Pipeline Integration

**Goal:** Run `fetch_ga4_stats.py` as part of the daily pipeline.

**Steps:**

1. In `dossier/generate.py`, add a stage that runs `dossier/fetch_ga4_stats.py`
2. Output goes to `landing/data/ga4_stats.json`
3. After pipeline completes, rsync landing/ to Vultr (or add to deploy step)
4. Token at `dossier/ga4_token.json` handles OAuth automatically

**Files:** `dossier/generate.py`, `dossier/fetch_ga4_stats.py`

---

## Priority 4: Factor Correlation Analysis (When Ready)

**Goal:** Once `track_record.json` has 30+ entries, analyze which scoring factors predict returns.

**Steps:**

1. Load `track_record.json` with forward returns
2. For each pick, pull the original score breakdown (stored in daily_picks.json)
3. Run correlation: `pandas.DataFrame.corr()` between each factor score and 5d/10d forward return
4. Output: ranked list of which factors actually matter (compare to current ML weights)
5. If results differ significantly from current weights, update `momentum_picks.py`

**Blocked on:** Need 30+ days of accumulated pick data first. Check `wc -l dossier/backtesting/track_record.json`.

---

## Voice Reference

See `VOICE.md` for Sam's persona. Commit messages: emoji prefix, patch-notes style.
