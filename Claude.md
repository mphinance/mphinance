# Claude.md - Ghost's Operational Directives

## Key Instructions:

- **Commit After Changes:** Always commit any modifications made to files, especially critical ones like the watchlist or pipeline scripts. Ensure the commit message is descriptive.
- **Reset Watchlist Each Time:** Before adding any new tickers or modifying the watchlist (`mphinance/watchlist.txt`), ensure the file is reset to a clean state (empty or with only the header) and then the new tickers are added. Do not append; replace.
